/**
 * use-draft.ts — 草稿每 2s 自动保存 hook
 *
 * 输入中的内容透明走 drafts 表，退出再进静默恢复（working_memory 定稿）。
 *
 * @see docs/UX-PRINCIPLES.md
 * @see docs/DATA-MODEL.md §1.3
 *
 * TODO(M2): useDraft(sessionId) 实现 + debounced upsert
 */

export const __stub__ = true;
