/**
 * capture-screen.tsx — 主输入界面
 *
 * 核心屏：文本输入 + 录音按钮 + 选图。Draft 每 2s 自动保存。
 * 启动 < 1s 到可输入；不等 reconcile 完成。
 *
 * @see docs/UX-PRINCIPLES.md
 * @see docs/ARCHITECTURE.md §7
 *
 * TODO(M2): 最小可输入 + 保存；M4 加录音；M5 加选图
 */

export const __stub__ = true;
