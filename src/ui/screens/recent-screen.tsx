/**
 * recent-screen.tsx — 最近记录列表
 *
 * 按 created_at DESC 列最近 N 条；每条附同步状态徽章。
 * 列表首屏 < 300ms（见 ARCHITECTURE.md §10）。
 *
 * @see docs/UX-PRINCIPLES.md
 *
 * TODO(M2/M4): 列表渲染 + 同步状态刷新订阅
 */

export const __stub__ = true;
