/**
 * time.ts — 时间工具
 *
 * isoNow / isoLocal（带时区） / parseIso / formatRelative
 *
 * 排序统一用 UTC ISO 字符串（字典序 = 时间序）；
 * 展示用 captured_at_local（带时区）。
 *
 * @see docs/DATA-MODEL.md §1.1
 *
 * TODO(M1): isoNow / isoLocal 基础实现
 */

export const __stub__ = true;
