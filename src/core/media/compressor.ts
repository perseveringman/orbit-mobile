/**
 * compressor.ts — 图片压缩
 *
 * 默认长边 2048px、质量 0.8（working_memory 定稿）。
 * expo-image-manipulator 实现。
 *
 * @see docs/ARCHITECTURE.md §8
 *
 * TODO(M5): compressImage(uri, opts?)
 */

export const __stub__ = true;
