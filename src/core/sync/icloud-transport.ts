/**
 * icloud-transport.ts — Layer 3 传输层
 *
 * 调 native/icloud-bridge.ts，把 captures/<id>/ 目录复制到
 * iCloud Drive container 的 inbox/<id>/，订阅上传状态和 processed/ 出现事件。
 *
 * @see docs/ARCHITECTURE.md §7
 * @see docs/SYNC-PROTOCOL.md
 *
 * TODO(M3): upload / subscribeAck / detectFailed
 */

export const __stub__ = true;
