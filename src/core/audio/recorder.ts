/**
 * recorder.ts — 录音
 *
 * 基于 expo-av / expo-audio。默认 AAC / 64kbps / 16kHz mono。
 * 保留原始录音文件到 capture 目录，供 Mac 端事后 Whisper 重处理。
 *
 * @see docs/ARCHITECTURE.md §8
 * @see docs/open-questions.md（是否默认保留原始录音 — 暂定 是）
 *
 * TODO(M4): start / stop / getRecording
 */

export const __stub__ = true;
