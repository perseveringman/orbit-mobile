/**
 * transcription.ts — 实时语音转写
 *
 * 优先 expo-speech-recognition（SFSpeechRecognizer）；中英混杂识别一般
 * 但免费、低延迟、无需联网。转写文本写进 manifest，原始音频保留。
 *
 * @see docs/ARCHITECTURE.md §9
 *
 * TODO(M4): startLive / stopLive / transcribeFile
 */

export const __stub__ = true;
